<% if institute.errors.any? %>
  <div class="error_display">
    <div class="alert alert-danger">
      The form contains <%= pluralize(institute.errors.count, "error") %>
    </div>
    <ul>
      <% institute.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
