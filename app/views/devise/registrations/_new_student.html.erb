<div class="row">
  <div class="offset-sm-1 col-sm-10 offset-sm-1">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { id: 'student_form' }) do |f| %>
      <%= devise_error_messages! %>

      <div class="form-row">
        <div class="form-group col-md-6">
          <%= f.label :name, class: 'col-form-label', for: 'student-name' %>
          <%= f.text_field :name, autofocus: true, class: 'form-control', id: 'student-name', placeholder: "Name" %>
        </div>

        <div class="form-group col-md-6">
          <%= f.label :email, class: 'col-form-label', for: 'student-email' %>
          <%= f.email_field :email, class: 'form-control', id: 'student-email', placeholder: "E-mail" %>
        </div>
      </div>
      
      <div class="form-row">
        <div class="form-group col-md-6">
          <%= f.label :dob, "D.O.B", class: 'col-form-label', for: 'student-dob' %>
          <%= f.date_field :dob, class: 'form-control', id: 'student-dob', placeholder: "Contact" %>
        </div>

        <div class="form-group col-md-6">
          <%= f.label :contact, class: 'col-form-label', for: 'student-contact' %>
          <%= f.text_field :contact, class: 'form-control', id: 'student-contact', placeholder: "Contact" %>
        </div>
      </div>

      <%= f.hidden_field :account_type, :value => "student" %>

      <div class="form-row">
        <div class="form-group col-md-6">
          <%= f.label :password, class: 'col-form-label', for: 'student-passwd' %>
          <% if @minimum_password_length %>
          <em>(<%= @minimum_password_length %> characters minimum)</em>
          <% end %>
          <%= f.password_field :password, autocomplete: "off", class: 'form-control', id: 'student-passwd', placeholder: "Password" %>
        </div>

        <div class="form-group col-md-6">
          <%= f.label :"Confirmation", class: 'col-form-label', for: 'student-confirm' %>
          <%= f.password_field :password_confirmation, autocomplete: "off", class: 'form-control', id: 'student-confirm', placeholder: "Confirm Password" %>
        </div>
      </div>

      <div class="actions">
        <%= f.submit "Sign up", class: 'btn btn-primary btn-block' %>
      </div>
    <% end %>
  </div>
</div>
